server:
  port: 1514

spring:
  datasource:
    username: root
    password: 123456
    url: jdbc:mysql:///xdb
  redis:
    port: 6379
    host: localhost

logging:
  level:
    com.tsumugii: debug

mybatis-plus:
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0